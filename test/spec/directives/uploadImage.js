'use strict';

describe('Directive: uploadImage', function () {
  beforeEach(module('sfsApp'));

  var element;

/*  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    element = angular.element('<div><upload-image></upload-image></div>');
    element = $compile(element)($rootScope);
    expect(element.html()).toBe(
      '<a href="#" class="thumbnail span4">' +
        '    <img ng-src="{{file.src}}"' +
        '     alt={{file.name}} ng-click="qrDecode(file.src)"></img>' +
        '</a>'
    );
  })); */
});
